{
  "name": "calendar-example",
  "version": "0.0.1",
  "private": true,
  "description": "Calendar booking app example with OAuth",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "packageManager": "pnpm@9.15.4"
}
